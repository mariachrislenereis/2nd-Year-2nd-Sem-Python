#sort the list using sorted function

list=[]

list=eval(input("Type your data (use comma between scores): "))

sorted_list=sorted(list)
print("The sorted data are ",sorted_list)


#reverse the list

print("The reversed data are ", sorted_list[::-1])
